# Next steps

1) add real data adapters (DefiLlama API + protocol APY source)
2) add cross-protocol correlation shock map (contagion proxy)
3) add notifier sink (telegram/email)
4) add markdown daily risk briefing generator
